apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
  - name: "vote-service"
    port: 8080
    targetPort: 80
    #nodePort: 30080
  selector:
    app: vote
  
